# Ops Log Artifacts

This directory stores operational evidence generated during guard mode and data latency incidents. Each subdirectory should follow the naming convention `<YYYYMMDD>_<context>` and contain JSONL exports that mirror the commands recorded in the associated runbooks.
