---

## 要件定義 (v1.2)

### 1. ステークホルダー

- **プロダクトオーナー**: 個人トレーダー（本プロジェクト発注者）
- **開発者**: Pythonエンジニア（Codex/LLM補助活用）
- **運用者**: 同上（自己運用）
- **コンプライアンスアドバイザ**: 国内外ブローカーの約款・規制に準拠して文言/ワークフローを監督
- **バックオフィス支援**: 将来の複数口座運用/損益集計を想定し、帳票・監査証跡をレビュー

### 2. 対象範囲・前提

- **対象市場**: 主要FXペア（USDJPY, EURUSD, GBPUSD, EURJPY を必須、その他は拡張）
- **時間足**: 5分/1時間（MVP）。必要に応じて1分/15分/日足は拡張
- **ベンチマーク**: 市販FXシグナルツール（裁量支援型）のSharpe/最大DD/提案スピードを常に上回ることを目標。四半期ごとにベンチマーク指標を更新。
- **稼働時間**: **PC稼働時のみ**（JST）。オフライン中は新規不可、起動時にCatch-up実施
- **環境**: **macOS + Python 3.11**（将来VPS移行可）
- **データ源**: **Dukascopy** もしくは **yfinance** からの5分足取得（選択式）。履歴CSVもサポート。**ブローカーAPIは将来拡張**（REST/WebSocket）。
- **規制**: 自己売買で投資助言に該当しないUI/文言に留意
- **KPI（優先度）**: 最大DD≦15% / Sharpe≧0.8・Sortino≧1.0 / 年率+12%（エクイティベース・税引前・取引コスト込み）。ベンチマーク比 α=+5%以上を目標値に追加。
- **運用ガードレール**: PC・ネットワーク異常時はFail-Safe停止、再開はRunbookに従って手順化。

### 3. 機能要件 (FR)

- **FR-01 データ取得**: 5分足を取得。`provider={yfinance|dukascopy}` で切替。yfinanceは `*=X` ティッカー（例: USDJPY=X）を使用し、\*\*短いリテンション（intradayは数十日）\*\*のため長期はDukascopyにフォールバック。履歴CSV（timestamp, open, high, low, close, volume[任意]）も可。

- **FR-02 足生成/品質**: 欠損補完・外れ値検知（閾値超でアラート）。

- **FR-03 指標/特徴量**: MA/EMA/RSI/MACD/ATR/BB/Donchian 等の計算。マルチタイムフレーム合成をサポート。

- **FR-04 シグナルエンジン**: ルール/モデルのプラグインIF。オン/オフ、重み付け、優先順位を設定可能。

- **FR-05 リスク管理**: 1トレード最大損失1%、日次-3%/週次-6%で自動停止(Kill Switch)、同時ポジション上限と相関管理。`risk_policy.yaml`で通貨バケット/レバレッジ別ポリシーを定義。

- **FR-06 サイジング**: Fixed Fractional を提供（MVP）。ATRベースVol Targetは将来拡張。

- **FR-07 注文/執行**: **自動発注は行わない（HITL）**。シグナル確定時に**注文チケット**（推奨Entry/Size/SL/TP/注文種別）を生成し、人間がブローカー画面で手動発注する。部分約定/リトライ/タイムアウト等の自動処理は将来の自動化フェーズで対応。

- **FR-08 モード切替**: Backtest / PaperTrade / LiveTrade の3モード。共通ロジックで挙動一致（価格/手数料/遅延の扱いを可能な限り同一化）。HITLフロー（提案→承認/却下/編集→監査）は全モードで同一。

- **FR-09 最適化**: グリッド/ランダム探索、期間分割（Walk-Forward）対応。

- **FR-10 レポート**: 成績指標（CAGR/DD/Sharpe/WinRate/PF）とエクイティ/DDカーブ、月次/週次集計（MVPは週次）。ライブでは週次レポート＋月次「プロトレーダーレビュー」PDFを生成。Liveモードでは約定実績CSV（`ticket_id, signal_id, fill_ts, fill_price, quantity, pnl, comment` を最低限含む）を取り込み、レポートと監査ログに突合反映する。

- **FR-11 ログ/監査**: シグナル→リスク→注文→約定のイベントを時系列JSONで永続化。設定変更も監査。Liveモードの実績取り込み時はチケットID/シグナルID・約定価格・約定タイムスタンプを必須フィールドとして検証し、HITL監査要件（人間承認チケットと実約定の整合/スリッページ記録/監査証跡保存）を満たすこと。

- **FR-12 アラート**: 異常（接続断、日次/週次損失到達、連続エラー）を通知（まずはメール、将来Slack）。

- **FR-13 カレンダーフィルタ**: 高インパクト経済指標前後の新規エントリー抑制（CSV連動、±15分）。

- **FR-14 設定管理**: YAML/JSONのプロファイル化（dev/paper/prod）。

- **FR-15 ロールオーバー/休日**: 祝日・週末の自動停止/再開、スプレッド拡大時間帯の制御。

- **FR-16 Resync/Catch-up**: 起動/再起動時にバックフィル→指標再計算→TTL/ドリフト/サイズ減衰でレイトエントリー判定。

- **FR-17 OCO常駐**: **ブローカー側のOCOを前提**。本ツールは**SL/TP値を提示**し、人間がブローカーに設定する（将来API接続時に自動配置へ拡張）。

- **FR-18 スナップショット**: 直近バー時刻、提案/承認/却下履歴、未失効チケット、設定ハッシュを周期保存し、再開時に整合。

- **FR-19 ハイブリッド最適化**: 直近ウィンドウ（6〜12ヶ月推奨）で“最近性能”を高めつつ、全期間で“堅牢性能”を維持する二目的最適化。例: `Score = w_recency·PF_recent + w_global·PF_all − λ·DD_all − γ·(1−Stability)`（既定 `w_recency=0.6, w_global=0.4`）。

- **FR-20 レジーム対応**: ADX・振幅・自相関等でトレンド/レンジ/高ボラ/低ボラのレジーム検出。サブ戦略の重みを**ソフトスイッチ**で連続調整（ヒステリシス有り）。

- **FR-21 安定化**: パラメータ近傍±10%摂動時の成績変動を抑える**安定性ペナルティ**を導入し、Stability Scoreを算出して最適化スコアに加味。

- **FR-22 ライブ健全性ガード**: ライブ/ペーパー時の勝率・期待Rの崩れを**SPRT**で逐次検定し、しきい値超で**提案頻度縮小/新規停止/アラート**を自動実行（閾値は設定可能）。**既定: α=0.05, β=0.10, H1: 勝率+5%/期待R+0.1, 停止後cooldown=24h**。異常停止時はRunbookステップIDを通知して再開条件を明示。

- **FR-23 コンフィグガバナンス/ホットリロード**: YAMLプロファイルの**スキーマ検証**＋**安全項目のホットリロード**、危険項目は次バー適用。**版管理/ロールバック/A-B（baseline vs shadow）切替**を提供。

- **FR-24 インストゥルメントメタデータ**: 各ペアの`pip_size`/`tick_size`/`price_decimals`/`min_lot`/`lot_step`を管理し、**SL/TP/指値の丸め**と**数量丸め**を自動適用（HITLチケットに反映）。

- **FR-25 データキャッシュ/再現性**: 市場データの**ローカルキャッシュ**、プロバイダのバージョン固定、**乱数seed/設定ハッシュ**をレポートに埋め込み、**バックテストの再現性**を担保。監査用に`data_manifest.json`を生成し、外部保管可能なZIPへエクスポート。

- **FR-26 経済カレンダー強化**: CSV/ICS経由でイベントを取り込み、\*\*重要度/通貨/発表時刻（UTC/JST）\*\*でフィルタ。**前後ウィンドウの動的拡張**（例: 超高インパクトは±30分）。

- **FR-27 コスト/執行モデル**: 時間帯・曜日・ボラに応じた**可変スプレッド**と**スリッページ分布**をモデル化。スリッページ分布は **pair × hour\_of\_day × regime** の**分位表（p10/p50/p90）****で保持。ロールオーバーは****新規禁止/上限スプレッド**を強制。**週末ギャップ**を**バックテストに注入**（ギャップ・シミュレーション）。M1はDukascopyティック/公開CSV/手動CSVから再構成したヒストリカル分位を利用し、リアルタイムブローカーAPIはM2以降の拡張とする。

- **FR-28 スワップ/ファンディング**: ロング/ショート別の**日次スワップ**（ブローカー近似テーブル）をBTに反映。\*\*水曜NYの3倍日（原則。祝日で変更あり）\*\*等の回避ルールを設定可能。M1は`config/swap_rates.csv`（手入力/公開CSV）を正とし、API連携はM2以降で対応。

- **FR-29 トリガー/フィルモデル**: Signal確定は**バー終値基準**（5m確定後n秒内に提示）。HITLの**人手遅延Δt**（例30–60秒）をBTで擬似注入。**MKT=次バー始値±滑り**、**LMT=到達しなければ未約定**（IOC扱いの可否を設定）。**上昇買い/下降売りの成行には正の滑り補正（選択的フィル偏り対策）を適用**。

- **FR-30 ヒューマンエラーフィルタ**: チケットに**桁/丸め/最小ロット検証**スタンプ（OK/NG）。**ダブルチェック表示**（方向・数量・SL/TP）。OCO未入力は**赤バッジ警告**。**承認後にTP/SL未入力がΔt>2min継続した場合、ボードに赤バナーを表示し新規提案を一時停止**。

- **FR-31 P&L通貨正規化**: P&Lとリスクは**口座通貨（JPY）**で統一。`fx_rates.parquet`に保存する5分足ミッドレート（優先度: yfinance → 直近成功値 → 手入力CSV）でクロスレート換算し、Backtest/Paper/Liveに共通ロジックを適用。

- **FR-32 データ検品/クロスチェック**: 主要区間を**二重取得**し、バー一致率（OHLC差分）を自動計測。閾値超は**差分レポート**。レポートは`reports/quality/<date>.md`に保存し、再取得結果を追記。

- **FR-33 チューニング・ガバナンス**: 週次更新の**最大変更幅±10%**、**冷却期間2週間**、異常時は**baselineへ自動ロールバック**。

- **FR-34 イベント別ウィンドウ**: impact∈{High,Med,Low}×currency一致で\*\*±{30,15,5}分\*\*を自動設定。**木曜NY**は保有跨ぎ禁止オプション。

- **FR-35 HL順序規則**: バー内は**エントリー→SL/TP**の順序（またはSL/TP優先を設定可能）で判定。**LMTはタッチ＝約定/未タッチ＝スキップ**。

- **FR-36 マージン/レバ制御**: `max_leverage` と `free_margin_min_pct` を設定。**free\_margin < 閾値**時は新規提案停止、**週末跨ぎ前**は総Rを `R_cross_max` 以下に圧縮。

- **FR-37 相関合算R**: 実効リスク `R_eff = Σ|R_i| + Σ_{i<j} ρ_ij·min(|R_i|,|R_j|)`（ρは直近90営業日のローリング相関）。`R_eff ≤ R_cap` をゲート条件に追加。

- **FR-38 Stop/Freeze距離適合**: `instruments.yaml` に `stop_level_pips` / `freeze_level_pips` / `min_stop_distance_pips` を保持。チケット提示時に **SL/TP/指値が最小距離を満たすか検証**し、未満なら**自動補正案**を併記（丸め後の値を提示）。

- **FR-39 保護付き成行（Marketable Limit）**: 成行推奨時も `protect_pips`（M1既定=3.0pips）を提示し、**成行相当の指値（買い=Ask+protect_pips / 売り=Bid−protect_pips）**で入力するガイドを表示。BTでは**未約定はスキップ**として扱い**過度な滑り**を防止。

- **FR-40 フィックス時間帯制御**: **東京9:55JST**、**ロンドン16:00（英国時間）**、**月末/四半期末Fix** をカレンダーに登録し、**±15分**（Highは±30分）で新規を禁止。

- **FR-41 スプレッド正常化クールダウン**: **スプレッドが閾値超**または大イベント後は、**連続N分（既定3分）****の****spread≤上限**を確認するまで**新規禁止**。

- **FR-42 Reduce-Onlyセーフティ**: **free\_margin < 閾値**、**R\_eff≥R\_cap**、**イベント窓内**等では、新規提案の代わりに**Reduce-Only**（縮小/クローズ提案）を提示。
- **FR-43 ストレステスト/シナリオ分析**: 主要イベント（例: 2016/06/24 Brexit, 2020/03 COVID Shock）のデータセットを同梱し、指定期間のリプレイ/感度分析（スプレッド倍率・滑り・遅延）を自動実行。結果はレポート化してRunbook更新に活用。
- **FR-44 トレードジャーナル/レビュー**: 承認済みチケットと実績を紐づけ、戦略別/レジーム別の振り返りダッシュボードを生成。ユーザーコメントを入力し、次回最適化時に参照。
- **FR-45 パラメータドリフト検知**: 最適化時のパラメータ分布と最新期間の使用パラメータを比較し、KLダイバージェンスしきい値超過で警告。必要に応じてベースラインへのロールバック提案を提示。
- **FR-46 ベンチマークリプレイ**: 外部ベンチマーク指標（例: 市販シグナル履歴）をCSVインポートし、同期間で自戦略と比較可能なレポートを生成。
- **FR-47 エマージェンシープロトコル**: データ停止・ブローカーダウン・異常スリッページ等を検知した際に、`emergency.yaml`に定義されたアクション（例: 全ポジションReduce-Only提案、Slack通知、再接続リトライ上限）を実行。
- **FR-48 運用健全性ダッシュボード**: CLI/将来GUIで、`HealthState`、`SpreadCooldownState`、`KillSwitch`、`Benchmark Gap`、`Journal Highlights`を統合表示。1画面で現状把握できるUXを提供。
- **FR-49 流動性アービトラージ検知**: **複数レートソース**（例: yfinance, Dukascopy, ブローカー試験API/CSV）を並列取得し、**Bid/Askの乖離・板厚変化・更新頻度**を統計監視。閾値超過時は**Stale Quoteアラート**を発火し、該当シンボルの新規提案を自動で`HOLD`に切り替える。レポートへ乖離グラフと再開判定コメントを添付。
- **FR-50 プレトレード・コンプライアンスチェック**: HITLチケット承認前に、**口座レバレッジ上限・建玉上限・ヘッジ規制**（FIFOなど）を照合。違反時は承認操作をブロックし、代替案（ポジション調整/Reduce-Only/サイズ縮小）を提示。承認者・理由を監査ログへ残す。
- **FR-51 キャピタル・アロケーションガード**: プロファイルごとに**日次・週次・月次のVaR/ES目標**と**許容資本配分**を設定し、提案時に累積Rを監視。ガード超過時は**提案頻度・最大サイズ**を自動的にスロットリングし、再開条件（残高回復、冷却期間）を提示。
- **FR-52 データプロベナンス/署名**: 取得データと生成レポートに**供給元・取得時刻・ハッシュ・署名者**を付与。`data_manifest.json`に**WORM保存**可能な署名ファイル（`manifest.sig`）を作成し、監査時に**完全性検証**できるようにする。

#### 3.1 マイルストーン別優先度（Functional Requirements）

- **M1 (MVP)**: FR-01〜FR-18, FR-23〜FR-25, FR-27〜FR-32, FR-38, FR-47, FR-48(サマリ表示のみ)
- **M2 (強化フェーズ)**: FR-19〜FR-22, FR-26, FR-33〜FR-35, FR-39〜FR-41, FR-42, FR-43, FR-44, FR-45, FR-49, FR-50
- **M3 (拡張フェーズ)**: FR-36〜FR-37, FR-46, FR-48(ベンチマーク差分チャート強化), FR-51, FR-52

### 4. 非機能要件 (NFR)

- **NFR-01 性能**: 5分/1時間足ストリームで**p95<100ms, p99<200ms**（ローカル環境基準）。測定点= `on_bar_in → board_render`、I/O非同期は除外。
- **NFR-02 信頼性**: 例外時フェイルセーフ、二重発注防止、自己復旧（再接続リトライ）。
- **NFR-03 可用性**: PC稼働中の運用時間帯で稼働率>99%。
- **NFR-04 セキュリティ**: APIキー暗号化保管（macOS Keychain）、最小権限。設定/鍵はバージョン管理から除外。
- **NFR-05 監査性**: すべての意思決定経路を追跡可能（監査ログと設定のハッシュ紐付け）。
- **NFR-06 可観測性**: メトリクス（約定率、遅延、スリッページ）とヘルスチェックのダッシュボード。`metrics/exporter.py`でPrometheus互換エンドポイント（ローカル）を提供。
- **NFR-07 運用性**: Runbook整備、カナリア運用手順、ワンクリックKill Switch。macOSはLaunchAgentで自動起動可。
- **NFR-08 タイムゾーン**: 既定は Asia/Tokyo、サマータイム相場に対応。
- **NFR-09 再現性**: すべてのレポートに `data_hash / config_hash / code_version / seed` を埋め込み、再実行で同一結果（許容差±0.1%）。Runbookで再現手順をステップ化し、外部PCでの検証手順を含める。
- **NFR-10 時刻整合**: NTP同期、バー境界のUTC整合（5分バーは00:00, 00:05…に整列）、夏時間による心拍ずれの回避。
- **NFR-11 UX/応答性**: CLI/GUIの主要操作応答 < 100ms、Signal Board の更新ジッタ ≤ 1s。
- **NFR-12 依存性固定**: `requirements.lock` と SHA256(SBOM) により依存性を固定。`pip --require-hashes` で検証し、**オフラインビルド手順**を提供。
- **NFR-13 メンテナビリティ**: コードは`black`/`ruff`準拠、ドキュメントはMkDocsで自動生成。モジュールごとに自動テストを整備し、重要ロジックのカバレッジ80%以上を維持。
- **NFR-14 レジリエンス**: プロバイダ/APIが60秒以上停止した場合にフェイルオーバー（データ再取得 or モード自動ダウングレード）。`emergency.yaml`にフェイルオーバー手順を定義。
- **NFR-15 UX一貫性**: CLI/将来GUIの主要操作は共通用語と配色規格（アクセントカラー、リスク警告は赤系#FF5F57）を遵守。UXガイドラインを付録化。
- **NFR-16 ナレッジ継承**: 週次Runbookレビューと月次振り返りテンプレートを整備し、改善サイクル（Plan-Do-Check-Act）を文書化。
- **NFR-17 セキュリティ強化**: macOS環境における**FileVault + Keychain**の活用、`config/secret/*.yaml`はAES-256で暗号化し、復号操作は監査ログ化。アクセス権は最小権限（`chmod 600`）とし、**四半期ごとに侵入テスト**（ローカル脆弱性スキャン）を実施。
- **NFR-18 データガバナンス**: 市場データ・実績ログは**90日ローカル保持 + 年次アーカイブ**を前提。アーカイブはWORM媒体（Write Once Read Many）に保存し、**復元テストを半期ごとに実施**。匿名化されたサマリーデータのみを外部共有可とする。
- **NFR-19 スケーラビリティ/負荷**: 主要4ペアに加えて**最大12ペア**までの拡張を想定。データパイプラインは`CPU<70%`、`メモリ<60%`を上限とし、プロファイル切替で**並列Backtest 4本**を24時間以内に完了できるアーキテクチャを維持する。

### 5. 外部インターフェース

- **データI/F**: OHLCV CSV（JST推奨、ISO8601/epoch秒）。
- **ブローカーI/F**: REST + WebSocket（抽象化レイヤーで業者差を吸収、第一候補 OANDA v20）。
- **UI/I/F**: CLI（Typer）をMVP、将来GUI/WEBは拡張。
- **経済カレンダーI/F**: CSV/ICS（UTC推奨）。必須列：`datetime,currency,impact,title`。時差変換と祝日扱いを定義。将来拡張でFastAPI Webhook取り込みを許容し、レート制限/認証方式を別紙に記載。
- **インストゥルメントI/F**: `instruments.yaml` で `pip_size,tick_size,price_decimals,min_lot,lot_step` を定義し、丸め/数量計算に利用。
- **ブローカー規則I/F**: `broker_rules.yaml` に `account_mode{netting|hedging}`, `fifo`, `margin_closeout_pct`, `negative_balance_protection`, `swap_triple_day`（既定: Wednesday\_NY）, `stop_level_pips/freeze_level_pips/max_lot` を定義。`risk_policy.yaml`/`emergency.yaml`と整合性チェックを行う。

### 6. 制約・前提 制約・前提

- **PC稼働時のみ**を前提（スリープ時は停止）。ネットワーク安定性はユーザ責任。UPS導入・停電時手順（手動/自動バックアップ）をRunbookに明記。
- 国内業者のAPI仕様差異はアダプタで吸収、第一弾は汎用的な海外ブローカーAPIを優先。

### 7. 範囲外 (Out of Scope)

- 税務計算、投資助言、コピー取引配信、複数口座間の裁定執行。
- ブローカーAPIによる完全自動発注（M3以降の別プロジェクトで検討）。

### 8. 受け入れ基準 (Acceptance Criteria)

- **AC-01**: サンプルCSVでバックテストが完走し、同一設定で再現誤差±0.1%以内。

- **AC-02**: PaperTradeで仮想約定ログが生成され、**OCO(SL/TP)** が期待通り常駐し保護が効く。

- **AC-03**: 日次-3%/週次-6%到達時に自動停止し、再開には手動操作が必要。

- **AC-04**: **Resync/Catch-up**でTTL=3×TF、ドリフト≤0.5R、減衰λ=0.1/minの仕様通りに動作（テストシナリオ3本以上パス）。Resync完了後に`data_manifest`と`HealthState`が整合していること。

- **AC-05**: 主要4ペアで5分/1時間足ストリーム処理が遅延<100msを満たす（ローカル基準）。

- **AC-06**: 監査ログから任意注文の意思決定経路（シグナル→リスク→執行）が追跡可能。

- **AC-07（ハイブリッド適応）**: `PF_recent > PF_all` かつ **PF\_all ≥ 1.10** を満たす。

- **AC-08（堅牢性）**: レジーム別（トレンド/レンジ/高低ボラ）で **PF ≥ 1.05**、かつコスト感度（スプレッド/スリッページ±100%）の**8割タイル**で **年率>0**・**MaxDD ≤ 15%**。

- **AC-09（安定性）**: パラメータ近傍±10%ランダム摂動（≥30サンプル）で **Stability Score ≥ 0.7**。

- **AC-10（HITL品質）**: 提案→承認/却下/編集→ログ保存の一連が**100試行で失敗0**。TTL切れ/ドリフト超過は自動失効。Liveモードでは実績CSV取り込み時に承認済チケットと突合し、未整合0件＋スリッページ集計と監査ログ更新が完了していること。

- **AC-11（丸め/最小単位適合）**: 4ペアで**桁・丸め・最小ロット**がブローカー仕様に適合（チケット検証関数でOK判定）。

- **AC-12（カレンダー）**: サンプル3イベントで**ブロック/解除**が仕様通り（超高インパクトは±30分に自動拡張）。

- **AC-13（再現性）**: 同一 `data_hash/config_hash/seed` でPF差異\*\*≤0.1%\*\*。

- **AC-14（SPRT）**: 失調シナリオで**新規停止**が自動発火（テスト用合成シリーズで検証）。

- **AC-15（ボード性能）**: 100提案/日の負荷で**更新遅延≤1s**、クラッシュ0、TTL表示誤差≤1s。

- **AC-16（スコア安定）**: パラメータ微小変更（±5%）で**トップ10のランク反転率≤10%**。

- **AC-17（コスト頑健）**: 時間帯別コストテーブル±100%で **PF>1.0維持**、**MaxDD増悪≤20%**。

- **AC-18（ギャップ耐性）**: 週末ギャップを過去分布でモンテカルロ注入し、**年率>0**を維持。

- **AC-19（先見排除）**: Bar-close確定後トリガーのBTと**リプレイ実行**の差異\*\*≤0.2%\*\*。

- **AC-20（操作エラー抑止）**: 100回の手動入力模擬で**重大入力ミス0**（検証関数で捕捉）。

- **AC-21（リスク一貫）**: 4ペアで1%リスク指定時の**実現R分布のブレ≤±5%**（換算誤差小）。

- **AC-22（データ一致）**: 同区間二重取得で**一致率≥99.5%**、不一致は**差分ログ**に列挙。

- **AC-23（過学習抑止）**: ローリング6ヶ月で、更新週の**次月PF低下率≤20%**。

- **AC-24（イベント抑止）**: サンプル10イベントで**新規提案ゼロ**／解除後は正常再開。

- **AC-25（BT一貫）**: HL順序切替による**PF差±5%以内**。

- **AC-26（Paper-Liveパリティ）**: 同一区間リプレイで**シグナル一致率≥99%**、R分布の**KS検定 p>0.1**。

- **AC-27（依存性固定）**: `pip --require-hashes`＋SBOM検証で**未ピン依存0**。

- **AC-28（データ品質アラート）**: 欠損率>0.5%/日は**自動停止**し、復旧後に**自動再開**。

- **AC-29（SPRTデフォルト）**: 既定の α=0.05, β=0.10 で停止が発火し、**cooldown 24h**後に解錠。

- **AC-30（OCO未設定抑止）**: 承認から2分以内に `oco_set=true` とならなければ**新規提案0**になることを確認。

- **AC-31（Stop/Freeze適合）**: 4ペア×50件のチケットで **最小距離/丸めの不一致0**（検証関数OK）。

- **AC-32（保護付き成行）**: 高ボラ想定のBTで Marketable Limit発注時の滑りが `protect_pips`（M1既定=3.0pips）を超えた約定が0件；未約定はスキップ扱いで整合。

- **AC-33（Fix窓抑止）**: 東京9:55/ロンドン16:00/月末Fixで **新規提案ゼロ**。

- **AC-34（スプレッドクールダウン）**: イベント直後の連続3分間**spread≤上限**が満たされるまでは **新規提案ゼロ**。

- **AC-35（Reduce-Only）**: free\_margin<閾値のテストで **新規提案0/Reduce-Only提示100%**。
- **AC-36（ストレステスト）**: Brexit/COVIDシナリオで最大DDが`baseline+5%`以内、提案遅延が5分TFで120秒以内。
- **AC-37（ジャーナル）**: 週次レポートにユーザーコメントと戦略別KPIが表示され、過去4週のドリフト指標と並べて閲覧できる。

- **AC-38（流動性ガード）**: 二重取得レートで**乖離>1.5×通常σ**が5分継続した際に新規提案が`HOLD`となり、解除前に手動承認が必要。解除時は乖離グラフ付きレポートが生成されること。

- **AC-39（コンプライアンスチェック）**: FIFO禁止口座やポジション上限を模擬したシナリオで、違反チケットが100%ブロックされ、代替案が提示される。監査ログに承認者IDとタイムスタンプが記録されていること。

- **AC-40（キャピタルガード）**: VaR/ES閾値を超過するバッチテストで、提案頻度が50%以上減衰し、冷却期間または残高回復で自動解除される。

- **AC-41（データ署名）**: `data_manifest.json`と`manifest.sig`の組でハッシュ検証が成功し、改ざんした場合は検証が失敗する自動テストを提供。

- **AC-42（緊急プロトコル）**: `emergency.yaml`の`data_outage`シナリオでReduce-Only提案＋アラートが3分以内に発火し、Kill Switchが`hard_stop`へ遷移する。

#### 8.1 マイルストーン別Acceptance Criteria

- **M1 (MVP)**: AC-01〜AC-12, AC-20, AC-22, AC-42
- **M2 (強化フェーズ)**: AC-13〜AC-19, AC-21, AC-23〜AC-30, AC-32〜AC-39
- **M3 (拡張フェーズ)**: AC-31, AC-33〜AC-41

### 9. マイルストーン

- **M0 (現行)**: 要件定義v1.1 完了
- **M1**: MVP（Backtest/Paper/MA+RSI/リスク/OCO/Resync/レポート/監査）
- **M2**: Donchian/平均回帰の追加、WFA最適化、Slack通知、ダウンタイムBT再現強化
- **M3**: LiveTrade(小額) + 監視/アラート強化 + ブローカー2社目対応



---

### 10. KPI・運用ガバナンス

- **KPIレビューリズム**: 週次で`PF_recent / PF_all / DD_recent / KillSwitch回数 / SpreadPenalty累積`をレビュー。月次で`Sharpe / Sortino / α (ベンチマーク差)`、四半期で`最大DD / Calmar / HitRate by Regime`を評価し、改善アクションをRunbookに追記する。
- **キャピタル配分会議**: 月初に**キャピタル・アロケーションガード（FR-51）**の閾値を確認。`VaR_95`, `ES_97.5`, `WorstCaseR`の3指標をダッシュボード化し、閾値超過時の削減戦略（Reduce-Only/サイズ縮小/戦略OFF）を決定する。議事録は`reports/governance/<YYYYMM>.md`に保管。
- **コンプライアンス監査**: 半期ごとに**プレトレード・コンプライアンスチェック（FR-50）**のルールセットを見直し、ブローカー約款更新がある場合は2週間以内に反映。監査ログからサンプリング検証を行い、逸脱0を確認する。
- **流動性ヘルスモニタ**: `liquidity_monitor.parquet`で記録された乖離と板厚の推移を週次で可視化。アービトラージ検知（FR-49）が3回以上発火した場合はデータソースの再評価とバックテストの再実行を義務化。
- **データ保全テーブルトップ演習**: 半期ごとに`manifest.sig`の検証手順を訓練し、復元所要時間・成功可否を記録。結果は`reports/drill/<YYYYMMDD>.md`にまとめ、次回改善項目を列挙する。
- **シグナル品質ゲート**: 月次にトップシグナル10件の`Reasoning Extract`と実績をレビューし、`badges`との整合を確認。ヒューマンエラー検出や誤差原因のフィードバックを戦略ルールに反映。

---

### 付録B: データ取得設定例 (YAML)

```yaml
# data.yaml
provider: yfinance   # yfinance / dukascopy / csv
symbols: [USDJPY, EURUSD, GBPUSD, EURJPY]
interval: 5m
start: 2024-10-01
end: 2025-10-01

# マッピング（yfinanceは “=X” ティッカー）
yfinance:
  ticker_map:
    USDJPY: USDJPY=X
    EURUSD: EURUSD=X
    GBPUSD: GBPUSD=X
    EURJPY: EURJPY=X
  # yfinanceはintradayの取得範囲が短いため（例: 5mは~60日）、長期はdukascopyを利用

# Dukascopyの設定例（1分足→5分足に集計する運用も可）
dukascopy:
  # 例: ローカルキャッシュ先など
  cache_dir: ./cache/dukascopy
  timeframe: 5m  # または 1m を取って resample: true
  resample_from_1m: false
```

````

---

### 追記: GUI化（ロードマップ/要件） v1.1 反映
- **目的**: 通知なしの“見に行く運用”に最適化した**Signal Board GUI**を提供。人間がチケット値（Entry/Size/SL/TP）を見てブローカーで手発注。
- **技術方針**: フロント **React + shadcn/ui + Tailwind**、配信 **WebSocket**（`/ws/signals`）。デスクトップ化は**Tauri**（macOS想定）。
- **機能（MVP）**:
  1) シグナル一覧（5m更新）/ フィルタ / ソート / スコア**強調表示**（HOT/STRONG）
  2) **注文チケットダイアログ**（Entry/Size/SL/TP/R/TTL/根拠バッジ、コピー）
  3) 設定パネル（主要パラメータの閲覧・保存）/ プロファイル切替（baseline/shadow）
- **非機能**: 起動→初回描画<1s、スクロール60fps目標、100提案連続操作でクラッシュ0。
- **アクセシビリティ**: 色覚多様性配慮（色＋アイコン/バッジの併用）、フォントは等幅欄を明示。
- **受け入れ基準（GUI）**:
  - AC-G1: WebSocket切断/再接続時に自動復旧、見逃し提案0
  - AC-G2: チケットの**クリップボードコピー**がブローカー画面にそのまま貼付可能
  - AC-G3: 設定変更が保存・復元でき、実効設定に反映（危険項目は次バー適用）
- **マイルストーン更新**:
  - **M1**: CLI版Signal Boardで運用開始（現行）
  - **M2**: GUI最小版（Web）+ `/ws/signals`連携 + チケットコピー
  - **M3**: **Tauri**でmacOSアプリ化、設定パネル/プロファイル切替、署名は任意



---

### 付録C: 設定スキーマ（抜粋・JSON Schema）
```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "cfg.schema.json",
  "type": "object",
  "required": ["timeframes", "risk", "gates", "strategies", "scoring"],
  "properties": {
    "timeframes": {"type": "object", "properties": {
      "trigger": {"type": "string", "enum": ["1m","5m","15m","1h","1d"]},
      "regime_ref": {"type": "string"}
    }, "required": ["trigger","regime_ref"]},
    "risk": {"type": "object", "properties": {
      "per_trade": {"type": "number", "minimum": 0.001, "maximum": 0.02},
      "sl_atr_mult": {"type": "number", "minimum": 1.0, "maximum": 3.0},
      "tp_R": {"type": "number", "minimum": 1.0, "maximum": 3.0}
    }, "required": ["per_trade","sl_atr_mult","tp_R"]},
    "gates": {"type": "object", "properties": {
      "spread_max_pips": {"type": "number", "minimum": 0.5, "maximum": 3.0},
      "news_block_minutes": {"type": "integer", "minimum": 0, "maximum": 120},
      "drift_max_R": {"type": "number", "minimum": 0.1, "maximum": 1.0},
      "ttl_factor": {"type": "integer", "minimum": 1, "maximum": 10}
    }, "required": ["spread_max_pips","news_block_minutes","drift_max_R","ttl_factor"]}
  }
}
````

### 付録D: 注文チケット仕様（HITL）

**フォーマット（JSON Lines / 監査ログ内にも同一で保存）**

```json
{
  "ts": "2025-10-18T12:05:00+09:00",
  "symbol": "USDJPY",
  "side": "LONG",
  "entry": 151.230,
  "size": 41000,
  "sl": 151.000,
  "tp": 151.590,
  "R": 0.240,
  "ttl_sec": 900,
  "badges": ["ALIGN","VOL","STAB"],
  "score": 84.2,
  "reason": "Donchian20 break / 1H up, ADX>25",
  "ui_checklist": {"lot_round_ok": true, "price_decimals_ok": true, "oco_set": false}
}
```

### 付録E: 監査ログイベント（JSON Lines）

- **event\_type**: `signal|ticket_view|approve|reject|edit|kill_switch|resume|cfg_change|autotune_update|actual_fill_imported|actual_fill_import_summary|actual_fill_import_failed`
- **共通フィールド**: `ts, event_id, session_id, cfg_hash, data_hash`
- **例**

```json
{"event_type":"approve","ts":"2025-10-18T12:07:12+09:00","symbol":"USDJPY","side":"LONG","size":41000,"sl":151.0,"tp":151.59,"user_note":"追随"}
```

### 付録F: テスト計画（ACに紐づくシナリオ）

- **TC-01**（AC-04）Resync: ダウン→復帰→TTL/ドリフト/減衰の判定が一致
- **TC-02**（AC-10）HITL E2E: 100件連続で承認/却下/編集→全成功。Live実績CSV（`ticket_id, signal_id, fill_ts, fill_price`含む）を取り込み、監査ログ`actual_fill_imported`とCSVの照合結果が未一致0件であることを確認。
- **TC-03**（AC-14）SPRT: 合成データで失調→自動停止
- **TC-04**（AC-17）可変コスト±100%: PF>1.0維持
- **TC-05**（AC-18）週末ギャップ注入: 年率>0維持
- **TC-06**（AC-22）データ一致率: 99.5%以上
- **TC-07**（AC-25）HL順序切替: PF差±5%以内
- **TC-08**（耐障害）ネット切断/高遅延/ディスク満杯を注入→安全に停止/復旧
- **TC-09**（プロパティ）サイズ丸めの単調性・Rの符号不変を自動検査

### 付録G: 運用Runbook（要旨）

- **デイリー**: 起動→Signal Board確認→必要なら発注→終了前に`run report --today`
- **週次**: `run report --weekly`→改善メモ記載→（任意）Auto-Tune=ON（Paper）
- **Kill Switch発火時**: 新規提案停止。翌営業日再開まで**休む**。原因分析テンプレに沿って記録。
- **再起動/復帰**: `resync`→スナップショット整合→未失効チケットの再提示
- **設定変更**: `tool set`でセッション上書き→`cfg save`でプロファイル化→`cfg rollback`可

### 付録H: エラーハンドリング/コード

- **ERROR-C01** データ欠損>閾値 → 該当バーをスキップ＋赤バナー
- **ERROR-C02** 指数計算失敗 → 直近nバー再計算。継続不可ならFail-Safe停止
- **ERROR-C03** コンフィグ検証NG → ロールバック（直前cfg\_hash）
- **ERROR-C04** 監査ログ書込失敗 → リトライ3回→停止（Write-Aheadで失注防止）

### 付録I: バージョニング/リリース

- **SemVer**: `MAJOR.MINOR.PATCH`。`main`（安定）/`dev`（開発）を分離。
- リリース時に `code_version` と `cfg_hash` を**レポートに刻印**。`CHANGELOG.md` で互換性を明示。
