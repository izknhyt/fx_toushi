# Pipeline Steps

Optional step configuration for FeaturePipeline (future packets).

Files should validate against `schema/pipeline_steps.schema.json`.
M1 defaults rely on `config/feature_pipeline.yaml` only.
